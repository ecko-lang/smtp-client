# smtp - an SMTP client (RFC 5321) written in pure Ecko.
#
# This entrypoint curates the public surface. `send` is the API; the wire
# plumbing in client.ecko - reply parsing, dot-stuffing, AUTH PLAIN, message
# building - stays internal, so it can change without breaking anyone.
export { send } from "./client.ecko"
